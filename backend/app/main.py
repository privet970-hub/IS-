"""FastAPI Main Application"""\nfrom fastapi import FastAPI\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.responses import JSONResponse\nfrom app.config import APP_NAME, APP_VERSION, API_PREFIX, DEBUG\nfrom app.routes import optimization\n\n# Create FastAPI app\napp = FastAPI(\n    title=APP_NAME,\n    version=APP_VERSION,\n    debug=DEBUG,\n    description=\"Intelligent Supply Chain Optimization - Route Optimization API\",\n)\n\n# CORS Middleware\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],  # TODO: Configure with allowed origins\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n# Include routers\napp.include_router(optimization.router)\n\n\n@app.get(\"/\")\nasync def root():\n    \"\"\"Root endpoint\"\"\"\n    return {\n        \"message\": \"Welcome to IS- (Intelligent Supply Chain Optimization)\",\n        \"version\": APP_VERSION,\n        \"docs\": \"/docs\",\n        \"redoc\": \"/redoc\",\n        \"openapi_schema\": \"/openapi.json\",\n    }\n\n\n@app.get(\"/health\")\nasync def health_check():\n    \"\"\"Health check endpoint\"\"\"\n    return {\"status\": \"healthy\"}\n\n\n@app.get(f\"{API_PREFIX}/status\")\nasync def api_status():\n    \"\"\"API status endpoint\"\"\"\n    return {\n        \"status\": \"operational\",\n        \"version\": APP_VERSION,\n        \"service\": APP_NAME,\n    }\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8000)\n