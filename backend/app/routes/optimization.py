"""Route Optimization API Endpoints"""\nfrom fastapi import APIRouter, HTTPException, status\nfrom typing import List\nfrom app.models.optimization import OptimizationRequest, OptimizationResponse\nfrom app.models.location import Location\nfrom app.services.route_optimizer import RouteOptimizerService\n\nrouter = APIRouter(\n    prefix=\"/api/v1/optimization\",\n    tags=[\"optimization\"],\n)\n\n# Initialize optimizer service\noptimizer_service = RouteOptimizerService()\n\n\n@router.post(\n    \"/optimize\",\n    response_model=OptimizationResponse,\n    summary=\"Optimize vehicle routes\",\n    description=\"Optimize delivery routes for multiple vehicles using Google OR-Tools\",\n)\nasync def optimize_routes(request: OptimizationRequest) -> OptimizationResponse:\n    \"\"\"\n    Optimize delivery routes for multiple vehicles.\n    \n    This endpoint takes a list of vehicles and delivery stops,\n    and returns optimized routes that minimize total distance and cost.\n    \n    **Request body:**\n    - vehicles: List of available vehicles with capacity and fuel info\n    - stops: List of delivery/pickup locations\n    - start_depot_id: Starting depot location ID\n    - end_depot_id: Ending depot location ID (optional)\n    - time_limit_seconds: Optimization time limit (default: 30s)\n    \n    **Response:**\n    - success: Whether optimization succeeded\n    - message: Status message\n    - routes: List of optimized routes per vehicle\n    - total_distance: Total distance across all routes (km)\n    - total_fuel_consumption: Total fuel consumption (L)\n    - total_cost: Total fuel cost (KRW)\n    - unserved_stops: Stops that couldn't be assigned to any vehicle\n    - optimization_time_ms: Time taken for optimization (ms)\n    \"\"\"\n    try:\n        # Find depot locations\n        start_depot = None\n        end_depot = None\n        \n        for stop in request.stops:\n            if stop.id == request.start_depot_id:\n                start_depot = Location(\n                    id=stop.id,\n                    name=stop.name,\n                    latitude=stop.latitude,\n                    longitude=stop.longitude,\n                )\n                break\n        \n        if not start_depot:\n            raise ValueError(f\"Start depot '{request.start_depot_id}' not found in stops\")\n        \n        # Get end depot if specified\n        if request.end_depot_id and request.end_depot_id != request.start_depot_id:\n            for stop in request.stops:\n                if stop.id == request.end_depot_id:\n                    end_depot = Location(\n                        id=stop.id,\n                        name=stop.name,\n                        latitude=stop.latitude,\n                        longitude=stop.longitude,\n                    )\n                    break\n            if not end_depot:\n                raise ValueError(f\"End depot '{request.end_depot_id}' not found in stops\")\n        else:\n            end_depot = start_depot\n        \n        # Filter out depot from stops list (depot should not be in delivery stops)\n        delivery_stops = [\n            stop for stop in request.stops\n            if stop.id != request.start_depot_id and stop.id != request.end_depot_id\n        ]\n        \n        if not delivery_stops:\n            return OptimizationResponse(\n                success=False,\n                message=\"No delivery stops found. Stops must be different from depot.\",\n            )\n        \n        if not request.vehicles:\n            return OptimizationResponse(\n                success=False,\n                message=\"At least one vehicle is required\",\n            )\n        \n        # Run optimization\n        response = optimizer_service.optimize(\n            vehicles=request.vehicles,\n            stops=delivery_stops,\n            start_location=start_depot,\n            end_location=end_depot,\n            time_limit_seconds=request.time_limit_seconds,\n        )\n        \n        return response\n        \n    except ValueError as e:\n        return OptimizationResponse(\n            success=False,\n            message=str(e),\n        )\n    except Exception as e:\n        raise HTTPException(\n            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,\n            detail=f\"Optimization error: {str(e)}\",\n        )\n\n\n@router.get(\n    \"/health\",\n    summary=\"Check optimization service health\",\n    description=\"Health check endpoint for optimization service\",\n)\nasync def health_check():\n    \"\"\"\n    Check if the optimization service is healthy and responsive.\n    \"\"\"\n    return {\n        \"status\": \"healthy\",\n        \"service\": \"route-optimization\",\n        \"version\": \"0.1.0\",\n    }\n"
